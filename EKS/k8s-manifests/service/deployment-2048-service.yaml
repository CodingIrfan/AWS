apiVersion: v1
kind: Service
metadata:
  name: deployment-2048-service
  namespace: default
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: app-2048
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80